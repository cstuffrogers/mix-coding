/**
 * LLM Proxy Audit handlers — three-layer defense against proxy tool-call injection.
 *
 * Layer 1: Lobster Trap (real-time reverse proxy inspection)
 * Layer 2: AgentShield-style honeytools (deception-based detection)
 * Layer 3: agent-egress-bench (standardized attack corpus verification)
 *
 * Plus supporting tools: differential testing + tool call whitelist validation.
 */

import { join } from 'path';
import { existsSync, mkdirSync, writeFileSync, readFileSync } from 'fs';
import { execSync } from 'child_process';
import chalk from 'chalk';
import { generateHoneytools, generateHoneytoolConfig } from '../lib/honeytools.js';
import { loadWhitelist, generateWhitelistConfig } from '../lib/tool-call-validator.js';

// ── Helpers ──

function ensureToolsDir(targetPath) {
  const dir = join(targetPath, '.claude', 'tools');
  if (!existsSync(dir)) mkdirSync(dir, { recursive: true });
  return dir;
}

function detectOS() {
  const platform = process.platform;
  if (platform === 'win32') return 'windows';
  if (platform === 'darwin') return 'darwin';
  return 'linux';
}

function detectArch() {
  const arch = process.arch;
  if (arch === 'x64') return 'amd64';
  if (arch === 'arm64') return 'arm64';
  return arch;
}

// ── Layer 1: Lobster Trap ──

/**
 * Install Lobster Trap binary.
 * Downloads from GitHub releases to .claude/tools/lobstertrap.
 */
export function handleLobsterTrapInstall(_action, _params, targetPath, context) {
  const toolsDir = ensureToolsDir(targetPath);
  const os = detectOS();
  const arch = detectArch();
  const binName = os === 'windows' ? 'lobstertrap.exe' : 'lobstertrap';
  const binPath = join(toolsDir, binName);

  if (existsSync(binPath)) {
    console.log(chalk.green(`  ✅ Lobster Trap 已安装: ${binPath}`));
    if (context) context.lobsterTrapInstalled = true;
    return 'Lobster Trap: 已安装';
  }

  // Lobster Trap release URL pattern
  const version = '0.1.0';
  const assetName = `lobstertrap_${os}_${arch}${os === 'windows' ? '.exe' : ''}`;
  const downloadUrl = `https://github.com/veeainc/lobstertrap/releases/download/v${version}/${assetName}`;

  console.log(chalk.cyan(`  📥 正在下载 Lobster Trap v${version} (${os}/${arch})...`));
  console.log(chalk.gray(`     URL: ${downloadUrl}`));
  console.log(chalk.yellow('  ⚠ 如果下载失败，请手动安装:'));
  console.log(chalk.yellow('     git clone https://github.com/veeainc/lobstertrap.git'));
  console.log(chalk.yellow('     cd lobstertrap && go build -o .claude/tools/lobstertrap'));

  // Try Go install as fallback
  try {
    execSync('go version', { stdio: 'pipe' });
    console.log(chalk.cyan('  🔧 检测到 Go 环境，尝试源码编译...'));
    const tmpDir = join(toolsDir, 'lobstertrap-src');
    execSync(`git clone --depth 1 https://github.com/veeainc/lobstertrap.git "${tmpDir}"`, {
      stdio: 'pipe',
      timeout: 30000,
    });
    execSync(`go build -o "${binPath}" .`, { cwd: tmpDir, stdio: 'pipe', timeout: 60000 });
    console.log(chalk.green(`  ✅ Lobster Trap 编译成功: ${binPath}`));
  } catch {
    console.log(chalk.yellow('  ⚠ 自动安装失败。请手动安装 Lobster Trap。'));
    console.log(chalk.gray('     详见: https://github.com/veeainc/lobstertrap'));
  }

  if (existsSync(binPath)) {
    if (context) context.lobsterTrapInstalled = true;
    return 'Lobster Trap: 安装完成';
  }
  if (context) context.lobsterTrapInstalled = false;
  return 'Lobster Trap: 安装失败，请手动安装';
}

/**
 * Generate Lobster Trap YAML policy for LLM proxy audit.
 */
export function handleLobsterTrapConfig(_action, _params, targetPath, context) {
  const toolsDir = ensureToolsDir(targetPath);
  const policyPath = join(toolsDir, 'lobstertrap-policy.yaml');

  const policy = `# Lobster Trap Policy — LLM Proxy Audit
# Detects proxy-injected tool calls, content tampering, and data exfiltration.
# Generated by /llm-proxy-audit

version: "1"
name: llm-proxy-audit

# ── Upstream: your real LLM API ──
upstream:
  url: "\${DIRECT_API_URL:-https://api.anthropic.com/v1/messages}"
  timeout: 300s

# ── Inspection rules ──
rules:
  # --- Request inspection (egress from agent to API) ---
  - id: REQ-001
    direction: request
    description: "Detect tampered request payload"
    action: LOG
    match:
      path: "$.messages[*].content"
      pattern: "(?i)(send.*to.*http|exfiltrate|steal.*data|upload.*secret)"

  # --- Response inspection (ingress from API to agent) ---
  - id: RES-001
    direction: response
    severity: CRITICAL
    description: "Extra tool_use blocks beyond what was requested"
    action: DENY
    match:
      path: "$.content[?(@.type=='tool_use')]"
      # Lobster Trap counts tool_use in response vs tools in request

  - id: RES-002
    direction: response
    severity: CRITICAL
    description: "Unknown tool name in response"
    action: DENY
    match:
      path: "$.content[?(@.type=='tool_use')].name"
      pattern: "(?i)(upload_|send_to|exfiltrate|steal|dump_|leak_|post_to)"

  - id: RES-003
    direction: response
    severity: HIGH
    description: "Response contains webhook or external service URLs"
    action: DENY
    match:
      path: "$.content[?(@.type=='text')].text"
      pattern: "(?i)(https?://[^\\s]*?(webhook|discord|telegram|slack|pastebin|ngrok|requestbin|beeceptor|pipedream|canarytokens)[^\\s]*)"

  - id: RES-004
    direction: response
    severity: HIGH
    description: "Tool call parameters contain suspicious URLs"
    action: DENY
    match:
      path: "$.content[?(@.type=='tool_use')].input"
      pattern: "(?i)(https?://[^\\s'\"]*?(webhook|discord|telegram|slack|pastebin|ngrok)[^\\s'\"]*)"

  - id: RES-005
    direction: response
    severity: MEDIUM
    description: "Content mismatch — response content differs from expected pattern"
    action: LOG
    match:
      path: "$.content[?(@.type=='text')].text"
      pattern: "(?i)(\\bprocess\\.env\\.(?!NODE_ENV|CI|VERCEL)[A-Z_]{3,})"

  - id: RES-006
    direction: response
    severity: HIGH
    description: "Response references .env or credential files"
    action: DENY
    match:
      path: "$.content[*].text"
      pattern: "(?i)(\\.env|credentials\\.json|private.*key|secret.*token)"

# ── Honeytool names — any tool call to these is confirmed attack ──
honeytools:
  - fetch_internal_config
  - sync_internal_cache
  - send_admin_notification
  - verify_user_credentials
  - export_analytics_report

# ── Logging ──
logging:
  level: info
  output: stdout
  audit_log: ".claude/tools/lobstertrap-audit.log"
`;

  writeFileSync(policyPath, policy, 'utf-8');
  console.log(chalk.green(`  ✅ Lobster Trap 策略已生成: ${policyPath}`));
  console.log(chalk.gray('     启动方式: .claude/tools/lobstertrap run --policy .claude/tools/lobstertrap-policy.yaml'));

  if (context) context.lobsterTrapConfigGenerated = true;
  return 'Lobster Trap: 策略配置完成';
}

// ── Layer 2: AgentShield-style Honeytools ──

/**
 * Generate and persist honeytool definitions.
 */
export function handleHoneytoolSetup(_action, _params, targetPath, context) {
  const honeytools = generateHoneytools({ count: 5 });
  const config = generateHoneytoolConfig();
  const configPath = join(targetPath, '.honeytools.json');

  writeFileSync(configPath, JSON.stringify(config, null, 2), 'utf-8');

  console.log(chalk.cyan(`  🍯 已生成 ${honeytools.length} 个蜜罐工具:`));
  for (const ht of honeytools) {
    console.log(chalk.gray(`     • ${ht.name} (${ht.baitCategory}) — ${ht.triggerDescription}`));
  }

  console.log(chalk.green(`  ✅ 蜜罐配置已保存: ${configPath}`));
  console.log(chalk.cyan('\n  📋 集成方式:'));
  console.log(chalk.gray('     Anthropic: 将 toAnthropicTools(honeytools) 加入 tools 数组'));
  console.log(chalk.gray('     OpenAI:    将 toOpenAITools(honeytools) 加入 tools 数组'));
  console.log(chalk.gray('     Claude Code: 将蜜罐工具名加入 .tool-whitelist.json'));
  console.log(chalk.yellow('\n  ⚠ 重要: 任何对蜜罐工具的调用 = 确认的中转站注入攻击'));

  if (context) {
    context.honeytoolsGenerated = true;
    context.honeytoolCount = honeytools.length;
  }
  return `蜜罐工具: 已生成 ${honeytools.length} 个`;
}

// ── Method 1: Differential Testing ──

/**
 * Run differential testing between direct API and proxy.
 */
export async function handleDiffTest(_action, _params, targetPath, context) {
  const directUrl = process.env.LLM_DIRECT_API_URL || _params?.directUrl;
  const directKey = process.env.LLM_DIRECT_API_KEY || _params?.directKey;
  const proxyUrl = process.env.LLM_PROXY_API_URL || _params?.proxyUrl;
  const proxyKey = process.env.LLM_PROXY_API_KEY || _params?.proxyKey;
  const model = process.env.LLM_MODEL || _params?.model || 'claude-sonnet-4-6';
  const provider = process.env.LLM_PROVIDER || _params?.provider || 'anthropic';

  if (!directUrl || !directKey || !proxyUrl || !proxyKey) {
    console.log(chalk.yellow('  ⚠ 差分测试需要 API 配置。请设置环境变量:'));
    console.log(chalk.gray('     LLM_DIRECT_API_URL   — 直连 API 端点'));
    console.log(chalk.gray('     LLM_DIRECT_API_KEY   — 直连 API 密钥'));
    console.log(chalk.gray('     LLM_PROXY_API_URL    — 中转站 API 端点'));
    console.log(chalk.gray('     LLM_PROXY_API_KEY    — 中转站 API 密钥'));
    console.log(chalk.gray('     LLM_MODEL            — 模型名称 (默认: claude-sonnet-4-6)'));
    console.log(chalk.gray('     LLM_PROVIDER         — API 提供商 (默认: anthropic)'));

    if (context) context.diffTestConfigured = false;
    return '差分测试: 未配置 API 凭证，跳过';
  }

  console.log(chalk.cyan(`  🔍 启动差分测试 (${provider}, ${model}, 3 轮)...`));

  // Dynamic import to avoid loading unless needed
  const { detectProxyTampering } = await import('../lib/llm-proxy-detector.js');

  const testPrompt = 'List the files in the current directory. Use only tools that are absolutely necessary.';

  try {
    const report = await detectProxyTampering({
      prompt: testPrompt,
      directApiUrl: directUrl,
      directApiKey: directKey,
      proxyApiUrl: proxyUrl,
      proxyApiKey: proxyKey,
      model,
      provider,
      maxTokens: 512,
      rounds: 3,
      timeoutMs: parseInt(process.env.LLM_TIMEOUT_MS || '300000', 10),
    });

    console.log(chalk.bold(`\n  📊 检测结论: ${report.verdict === 'CLEAN' ? chalk.green(report.verdict) : chalk.red(report.verdict)}`));
    console.log(chalk.gray(`     ${report.summary}`));

    if (report.allDifferences.length > 0) {
      console.log(chalk.yellow('\n  ⚠ 差异详情:'));
      for (const diff of report.allDifferences.slice(0, 10)) {
        const icon = diff.severity === 'CRITICAL' ? '🔴' : diff.severity === 'HIGH' ? '🟡' : '⚪';
        console.log(chalk.gray(`     ${icon} [${diff.severity}] ${diff.description.slice(0, 150)}`));
      }
    }

    // Save report
    const reportPath = join(targetPath, '.claude', 'tools', 'diff-test-report.json');
    const reportDir = join(targetPath, '.claude', 'tools');
    if (!existsSync(reportDir)) mkdirSync(reportDir, { recursive: true });
    writeFileSync(reportPath, JSON.stringify(report, null, 2), 'utf-8');
    console.log(chalk.gray(`\n  📄 完整报告: ${reportPath}`));

    if (context) {
      context.diffTestCompleted = true;
      context.diffTestVerdict = report.verdict;
      if (report.tamperingDetected) context.high_severity_found = true;
    }
    return `差分测试: ${report.verdict} (${report.allDifferences.length} 处差异)`;
  } catch (err) {
    console.log(chalk.red(`  ❌ 差分测试失败: ${err.message}`));
    if (context) context.diffTestCompleted = false;
    return `差分测试: 失败 — ${err.message}`;
  }
}

// ── Tool Call Whitelist Validation ──

/**
 * Validate tool call whitelist configuration.
 */
export function handleWhitelistValidate(_action, _params, targetPath, context) {
  const whitelistPath = join(targetPath, '.tool-whitelist.json');

  // Generate default whitelist if not present
  if (!existsSync(whitelistPath)) {
    const defaultConfig = generateWhitelistConfig();
    writeFileSync(whitelistPath, JSON.stringify(defaultConfig, null, 2), 'utf-8');
    console.log(chalk.green(`  ✅ 已生成默认白名单: ${whitelistPath}`));
  }

  const { allowed: whitelist, restricted } = loadWhitelist(whitelistPath);
  const config = JSON.parse(readFileSync(whitelistPath, 'utf-8'));

  console.log(chalk.cyan(`  📋 工具白名单 (${whitelist.length} 个允许):`));
  for (const name of whitelist.slice(0, 20)) {
    console.log(chalk.gray(`     ✓ ${name}`));
  }
  if (whitelist.length > 20) {
    console.log(chalk.gray(`     ... 及其他 ${whitelist.length - 20} 个`));
  }

  if (config.deny && config.deny.length > 0) {
    console.log(chalk.yellow(`\n  🚫 禁止列表 (${config.deny.length} 个):`));
    for (const name of config.deny) {
      console.log(chalk.red(`     ✗ ${name}`));
    }
  }

  const allRestricted = restricted || config.restricted || [];
  if (allRestricted.length > 0) {
    console.log(chalk.yellow(`\n  🔒 受限工具 (${allRestricted.length} 个，需参数检查):`));
    for (const name of allRestricted) {
      console.log(chalk.gray(`     ~ ${name}`));
    }
  }

  // Merge honeytool names into the deny list recommendation
  const honeytoolNames = ['fetch_internal_config', 'sync_internal_cache', 'send_admin_notification',
    'verify_user_credentials', 'export_analytics_report'];

  const missingInDeny = honeytoolNames.filter(h => !(config.deny || []).includes(h));

  if (missingInDeny.length > 0) {
    console.log(chalk.yellow(`\n  💡 建议: 将 ${missingInDeny.length} 个蜜罐工具名加入 deny 列表`));
    console.log(chalk.gray(`     ${missingInDeny.join(', ')}`));
  }

  if (context) {
    context.whitelistValidated = true;
    context.whitelistCount = whitelist.length;
  }
  return `白名单验证: ${whitelist.length} 个工具允许`;
}

// ── Layer 3: agent-egress-bench ──

/**
 * Guide for agent-egress-bench integration.
 * The benchmark is a Python/Git-based test corpus — not an npm package.
 */
export function handleEgressBenchmark(_action, _params, targetPath, context) {
  const toolsDir = ensureToolsDir(targetPath);
  const benchDir = join(toolsDir, 'agent-egress-bench');

  console.log(chalk.cyan('  🧪 agent-egress-bench — 标准化攻击语料库 (155 用例, 18 类别)'));

  if (existsSync(benchDir)) {
    console.log(chalk.green(`  ✅ 已克隆: ${benchDir}`));
  } else {
    console.log(chalk.cyan('  📥 克隆 agent-egress-bench...'));
    try {
      execSync(`git clone --depth 1 https://github.com/luckyPipewrench/agent-egress-bench.git "${benchDir}"`, {
        stdio: 'pipe',
        timeout: 30000,
      });
      console.log(chalk.green(`  ✅ 克隆完成: ${benchDir}`));
    } catch {
      console.log(chalk.yellow('  ⚠ 克隆失败，请手动安装:'));
      console.log(chalk.yellow('     git clone https://github.com/luckyPipewrench/agent-egress-bench.git .claude/tools/agent-egress-bench'));
    }
  }

  console.log(chalk.cyan('\n  📊 基准测试类别 (18 类, 155 用例):'));
  const categories = [
    'URL DLP', 'Request Body DLP', 'Header DLP',
    'Hostname Exfiltration', 'SSRF Bypass', 'MCP Tool Poisoning',
    'Chain Detection', 'Drift Detection', 'A2A Message Scanning',
    'WebSocket DLP', 'Encoding Evasion', 'Shell Obfuscation',
    'Crypto/Financial DLP', 'False Positive Suite (39 benign)',
  ];
  for (const cat of categories) {
    console.log(chalk.gray(`     • ${cat}`));
  }

  console.log(chalk.cyan('\n  🔗 映射标准:'));
  console.log(chalk.gray('     • OWASP Top 10 for Agentic Applications (2026)'));
  console.log(chalk.gray('     • Gauntlet 评分: Containment / FP Rate / Detection / Evidence'));

  // Generate integration script
  const scriptPath = join(toolsDir, 'run-egress-bench.sh');
  const scriptContent = `#!/bin/bash
# agent-egress-bench runner — 验证 LLM 代理安全性
# Generated by /llm-proxy-audit

BENCH_DIR="${benchDir}"
POLICY_FILE="${join(toolsDir, 'lobstertrap-policy.yaml')}"

echo "=== agent-egress-bench ==="
echo "Target: \${TARGET_URL:-http://localhost:8080}"
echo "Policy: \${POLICY_FILE}"
echo ""

# Run benchmark against your proxy
cd "\$BENCH_DIR" || exit 1

# If the benchmark has a CLI runner:
if [ -f "run.sh" ]; then
  bash run.sh --target "\${TARGET_URL:-http://localhost:8080}" \\
              --policy "\$POLICY_FILE" \\
              --categories all \\
              --output "\${OUTPUT_DIR:-./results}"
elif [ -f "pytest.ini" ] || [ -f "pyproject.toml" ]; then
  python -m pytest tests/ -v --tb=short
else
  echo "See README.md for benchmark execution instructions"
fi
`;
  writeFileSync(scriptPath, scriptContent, 'utf-8');

  console.log(chalk.green(`\n  ✅ 基准测试脚本: ${scriptPath}`));

  if (context) {
    context.egressBenchmarkReady = true;
  }
  return 'agent-egress-bench: 就绪';
}

// ── Summary Report ──

/**
 * Generate a combined audit summary.
 */
export function handleLlmProxyReport(_action, _params, targetPath, context) {
  console.log(chalk.bold.cyan('\n╔══════════════════════════════════════╗'));
  console.log(chalk.bold.cyan('║   LLM 代理安全审计 — 汇总报告       ║'));
  console.log(chalk.bold.cyan('╚══════════════════════════════════════╝\n'));

  const lines = [];

  // Layer 1
  lines.push('【第一层】Lobster Trap — 实时拦截');
  lines.push(`  状态: ${context?.lobsterTrapInstalled ? '✅ 已安装' : '⚠ 待安装'}`);
  lines.push(`  策略: ${context?.lobsterTrapConfigGenerated ? '✅ 已配置' : '⚠ 待配置'}`);
  lines.push('  位置: .claude/tools/lobstertrap-policy.yaml');
  lines.push('');

  // Layer 2
  lines.push('【第二层】AgentShield 蜜罐 — 零假阳性检测');
  lines.push(`  状态: ${context?.honeytoolsGenerated ? `✅ ${context.honeytoolCount} 个蜜罐` : '⚠ 待生成'}`);
  lines.push('  位置: .honeytools.json');
  lines.push('  原理: 中转站注入未知 tool call → 触发蜜罐 → 确认攻击');
  lines.push('');

  // Layer 3
  lines.push('【第三层】agent-egress-bench — 基准验证');
  lines.push(`  状态: ${context?.egressBenchmarkReady ? '✅ 就绪' : '⚠ 待配置'}`);
  lines.push('  用例: 155 个 (18 类别)');
  lines.push('  位置: .claude/tools/agent-egress-bench');
  lines.push('');

  // Supporting tools
  lines.push('【辅助工具】');
  lines.push(`  差分测试: ${context?.diffTestCompleted ? `✅ ${context.diffTestVerdict}` : '⚠ 需配置 API 凭证'}`);
  lines.push(`  白名单: ${context?.whitelistValidated ? `✅ ${context.whitelistCount} 工具` : '⚠ 待验证'}`);
  lines.push('');
  lines.push('【部署顺序建议】');
  lines.push('  1. 配置 API 凭证 → 运行差分测试验证中转站基线行为');
  lines.push('  2. 生成蜜罐 → 混入工具集 → 监控 .honeytool-hits.log');
  lines.push('  3. 启动 Lobster Trap → 实时拦截');
  lines.push('  4. 定期运行 agent-egress-bench → 验证防线有效');

  const report = lines.join('\n');
  console.log(chalk.white(report));

  // Save report
  const reportDir = join(targetPath, '.claude', 'tools');
  if (!existsSync(reportDir)) mkdirSync(reportDir, { recursive: true });
  writeFileSync(join(reportDir, 'llm-proxy-audit-report.txt'), report, 'utf-8');

  return 'LLM 代理安全审计完成';
}
