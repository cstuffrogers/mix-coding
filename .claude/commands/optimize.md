---
description: Launch performance optimization: problem selection → baseline → anti-pattern detection → measure → confirm.
---

启动性能优化场景。执行问题类型选择 → 基线测试 → 反模式识别 → 测量确认 → 增量测试 → 审查验证 → 效果确认 → 通知。

核心原则：无测量不优化，先定位瓶颈再修复，必须有 before/after 数字。

用法：/optimize
