---
description: Launch code refactor scenario: analyze structure, create refactor plan, incremental refactor, ensure tests pass.
argument-hint: "[目标模块]"
---

启动代码重构场景。分析代码结构，制定重构计划，渐进式重构，确保测试通过。

核心原则：重构不改变外在行为，只改善内部结构。

用法：/refactor [目标模块]
