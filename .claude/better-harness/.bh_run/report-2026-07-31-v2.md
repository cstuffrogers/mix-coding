# Better Harness Task-Loop Report (2026-07-31 v2)

> 证据驱动复审（第二轮）。基于 `evidence-2026-07-31-v2.json` 证据包 + 对本轮 3 项修复证据的独立核对（`.husky/pre-commit`、`CLAUDE.md` 执行原则、`start.js` printSummary 验收三元组）。对比第一轮报告 `report-2026-07-31.md`。

## At a Glance

- **Loop Effectiveness: 66/100**（上轮 62；+4。Change Validation 的「机械强制」与「post-edit 验证 lead」获得实质代码证据，但严格 Loop Effectiveness 口径仍需「可比后续任务结果」，故小幅上升不跃升）
- **Asset Health / Repair Progress: 68/100**（上轮 58；+10。6 项 finding 现 4 verified / 1 partial / 1 pending；finding #1 机械强制层从「未做」升级到「成立」，finding #4 从「纯 lead」升级到「有规则+提示块双证据」——计数未变但质量提升）
- **Demonstrated autonomy radius**: 受限改善（Change Validation 机械强制门禁成立；Reliable Delivery 仍无 reviewed-check 闭环；Learning Capture 仍无可比后续窗口）
- **Strongest loop**: Change Validation（唯一从「有证据」继续向「机械强制」推进的维度）
- **Largest observed leak**: Reliable Delivery（验收仍为 assistant-handoff，验收三元组是「提示块」不强制 agent 行为，reviewed-check 闭环仍缺）
- **Top expected gain**: 把验收三元组从「提示」升级为「绑定到 reviewed relevant check 的结果」（硬约束），关闭 Reliable Delivery 最大缺口

## 修复证据独立核对（全部确认）

1. **`.husky/pre-commit`** — 已从 `npx eslint claude-scene/src/ --max-warnings 500` 改为 `(cd claude-scene && npx eslint .)`。独立跑 `npx eslint .` → `0 errors, 21 warnings, EXIT=0`，error 现会阻断提交。
2. **`CLAUDE.md` 执行原则段** — line 91 已写入 **Post-edit 验证** 规则。
3. **`start.js` printSummary** — line 216-221 已加「验收三元组」提示块，对所有非 dry-run 场景收尾输出。

## Five Lifecycle Dimensions

| Dimension | What the evidence proves | Evidence boundary | Summary | Boundary / blocker |
| --- | --- | --- | --- | --- |
| Task Understanding | 1 observed + 1 connected signal。失败路由已收敛为可测 `detectHandlerFailure`（上轮）。 | 本轮未新增该维度证据。 | 任务边界清晰，失败路由可测。 | not observed（无后续可比结果） |
| Controlled Execution | 1 observed + 1 connected signal。start/list/show/fork 已 smoke 测。 | destructive-git 仍是用户声明。 | 执行入口稳定；destructive-git 强制未变。 | not observed（无新增闭环证据） |
| Change Validation | 25 tracked test 文件。**本轮新增机械强制证据**：pre-commit 用 claude-scene eslint config，error 阻断（EXIT=0/0 errors）。**Post-edit 验证现有规则证据**：CLAUDE.md line 91。vitest 26 文件 226 测试全过。 | evidence 包「43 编辑后无验证命令」是**历史会话**证据，早于本轮规则写入，不否定规则也不证明已生效。 | 机械强制门禁成立；post-edit 验证从「无规则」升级为「有规则+提示块」。 | 历史会话 lead 仍待后续窗口验证；±50-line / cognitive-complexity 仍靠人工。 |
| Reliable Delivery | 0 observed + 1 connected signal（4 CI workflow）。`closedEpisodeCount=0`、`reviewedCandidateCount=0`、`comparableModelOutcomeEvidence=false`。**本轮新增验收三元组提示块** + CLAUDE.md 三元组规则。 | 提示块是 console.log 输出，**不强制 agent 行为**。evidence 包 DELIVERY EVIDENCE「No bounded evidence observed」未变。 | 验收从「纯 handoff」升级为「handoff + 结构化提示」，但仍是软约束，未绑定到 reviewed check 结果。 | 提示块非机械强制；`withReviewedRelevantCheck` 仍为 0；branch protection / CODEOWNERS 未验证。 |
| Learning Capture | `state="N/A"`、`interventions=[]`。9 unscoped Skill 观察 0 recurring。 | `Effectiveness=pending-no-later-window`。 | 本轮修复尚未被「可比后续任务结果」验证为可复用改进。 | 无后续可比结果前不可评分；Memory Governance=checked-clean。 |

## Asset Health / Repair Progress（6 项 finding 跟踪）

| # | Finding | 上轮 | 本轮 | 证据变化 |
|---|---|---|---|---|
| 1 | declared change-safety 边界未机械强制 | Partial | **Partial（机械强制层成立）** | pre-commit 从 `--max-warnings 500` 改为 `cd claude-scene && npx eslint .`，error 现机械阻断。但 ±50-line / cognitive-complexity 仍靠人工，整体仍 Partial。 |
| 2 | session validation 不闭环到 reviewed check | Pending | **Pending（未变）** | `withReviewedRelevantCheck=0` 未变。验收三元组提示块改善「提示」而非「闭环到 reviewed check 结果」。 |
| 3 | Learning Capture 缺可比后续窗口 | 未评分 | **未评分（未变）** | `state="N/A"`、`pending-no-later-window`。需第二个观察窗口。 |
| 4 | 会话级 post-edit 验证 lead | Low（lead） | **Low→Medium（有规则证据）** | CLAUDE.md Post-edit 规则 + start.js 验收三元组提示块。从「无规则 lead」升级为「有规则+提示块」。但历史会话证据不能证明规则已改变 agent 行为，故未达 Verified。 |
| 5 | runStep 失败检测正则无测试 | Verified | **Verified（延续）** | `failure-detection.js` + 8 用例 + vitest 226/226。 |
| 6 | 5 个高 churn handler 无聚焦测试 | Verified | **Verified（延续）** | 5 个 handler 测试文件存在。 |

**汇总：4 verified / 1 partial / 1 pending**（计数与上轮同，但 finding #1 机械强制层从「未做」→「成立」，finding #4 从「纯 lead」→「有规则证据」——质量提升）。

## 剩余 Findings（仍存在）

1. **declared change-safety 边界部分机械强制（Partial，改善未全闭）** — Medium。lint error 现机械阻断，但 ±50-line / 0-new-dependency / ≤130% complexity 仍靠人工；cognitive-complexity 仍 per-file 禁用；根 eslint.config.js 与 claude-scene config 不一致（339 error 债）。
2. **session validation 不闭环到 reviewed check（Pending，未变）** — Medium。验收三元组提示块 + CLAUDE.md 规则是软约束，未绑定到 reviewed relevant check 结果。
3. **Learning Capture 缺可比后续窗口（未评分，未变）** — Low。需第二个观察窗口。
4. **会话级 post-edit 验证行为（Low→Medium，有规则未验证生效）** — Medium。规则已写入，但历史会话证据不能证明规则已改变 agent 行为，需后续窗口验证。

## 诚实评估：修复权重与证据门槛

- **#1 pre-commit 收紧（高权重，质变）**：从「error 不阻断」到「error 阻断」是硬约束质变，让 Change Validation 的「机械强制层」跨过证据门槛。
- **#4 post-edit 验证规则（中权重，待验证生效）**：从「无规则 lead」升级到「有规则+提示块」。但 CLAUDE.md 规则 + console.log 提示块都是软约束，依赖 agent 遵守。
- **#2 验收三元组提示块（低权重，软约束）**：console.log 输出，agent 可输出却不执行或忽略。改善「交付结构化提示」而非「闭环到 reviewed check 结果」。Reliable Delivery 维度因此未跨过证据门槛。
- **Loop Effectiveness 严格口径**：需「可比后续任务结果」。本轮是代码层证据，改善了 Change Validation 的「机械强制」子项，但尚无「可比后续任务结果」窗口。故 Loop Effectiveness 仅 +4，不跃升。

## 整体结论（相比第一轮）

- **Asset Health 58 → 68（+10）**：finding #1 机械强制层「未做」→「成立」，finding #4「纯 lead」→「有规则证据」。计数未变但质量提升。
- **Loop Effectiveness 62 → 66（+4）**：Change Validation 机械强制门禁成立。但严格口径下仍需「可比后续任务结果」，小幅上升不跃升。
- **5 维度**：Change Validation 继续向「机械强制」推进；Reliable Delivery 因验收三元组提示块从「纯 handoff」升级为「handoff + 结构化提示」，但仍是软约束，未跨过 reviewed-check 闭环门槛；其余 3 维度证据状态与上轮基本一致。
- **最大剩余杠杆**：Reliable Delivery 的 reviewed-check 闭环——把验收三元组从「提示」升级为「绑定到 reviewed relevant check 结果的硬约束」。
- **诚实边界**：本轮 3 项修复中，#1 是硬约束质变（高权重），#4 是规则写入（中权重，待验证生效），#2 是提示块（低权重，软约束）。better-harness 对「软约束」保守——提示块和 CLAUDE.md 规则不等于 agent 行为改变，需后续窗口验证。
