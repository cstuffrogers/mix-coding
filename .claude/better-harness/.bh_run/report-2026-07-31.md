# Better Harness Task-Loop Report (2026-07-31)

> 证据驱动复审。基于 `evidence-2026-07-31.json` 证据包 + 对修复证据的独立核对（failure-detection.js、start.js initLog、5 个 handler 测试文件、eslint、vitest、hallmark SKILL.md 断链）。对比上次报告 `report.md`（2026-07-30）。

## At a Glance

- Loop Effectiveness: 62/100（上次 54；+8。本轮修复使 Change Validation 维度从「未观察」进入「有证据」，但尚无可比的后续任务结果验证 loop 改进，故仅小幅上升）
- Asset Health / Repair Progress: 58/100（上次 0；6 项 pending 中现 4 verified、1 partial、1 pending）
- Demonstrated autonomy radius: 受限观察（Change Validation 有可验证的修复证据；Reliable Delivery / Learning Capture 仍无闭环证据）
- Strongest loop: Change Validation（本轮唯一从「未观察」变为「有证据」的维度——失败检测可测、高 churn handler 有聚焦测试、lint 归零）
- Largest observed leak: Learning Capture（仍无两个可比观察窗口，改进无法被验证为可复用）
- Top expected gain: 建立「带变更的验收路由绑定到 reviewed check」——这是 Reliable Delivery 维度剩余的最大杠杆

## What You Can Rely On Today

- 引擎核心 pass/fail 边界（`detectHandlerFailure`）现在有 8 个聚焦用例覆盖，不再仅靠间接验证。
- 5 个最高 churn handler（design / external-tool-checks / handler-verify / review / ui-tools）现有聚焦测试文件，路径级回归可被捕获。
- 执行日志每行带 per-run `run_id`（ts + randomBytes hex），同分钟/并发同场景运行可被可靠关联。
- lint 0 errors（21 warnings，--max-warnings 500 仍不阻断，但 error 已归零）；26 文件 / 226 测试全过。

## What You Gain Next

- 把「带变更的验收」从 assistant-handoff 升级为「绑定到 reviewed relevant check 的结果」，关闭 Reliable Delivery 维度的最大缺口。

---

## Five Lifecycle Dimensions

| Dimension | What the evidence proves | Evidence boundary | Summary | Boundary / blocker |
| --- | --- | --- | --- | --- |
| Task Understanding | 1 observed + 1 connected signal（6 agent guidance 文件、14 architecture/readme + 1 spec）。per-scene 失败路由仍编码在 handler 返回字符串里，但失败检测已被提取为可测的 `detectHandlerFailure`。 | evidence 包「Task understanding」事实行；本次会话未新增该维度证据。 | 任务边界清晰，失败路由从「自由文本正则」收敛为「可测函数」，但尚无可比后续任务证明路由质量提升。 | not observed（无后续可比结果） |
| Controlled Execution | 1 observed + 1 connected signal（4 workspace manifest、2 个 Task 工作入口）。start/list/show/fork 已 smoke 测。 | evidence 包「Controlled execution」事实行；--dry-run 与 smoke 测延续上次。 | 执行入口稳定；destructive-git 仍是用户声明而非机械强制（未变）。 | not observed（无新增闭环证据） |
| Change Validation | 25 个 tracked test 文件 + workspace test 脚本。本轮新增：`failure-detection.js` + 8 用例、5 个高 churn handler 聚焦测试、`LOG_RUN_ID` per-run 关联。eslint `.` EXIT=0（0 errors）。vitest 26 文件 / 226 测试全过。 | 独立核对：Read `failure-detection.js`、Grep `start.js` LOG_RUN_ID、ls 测试文件、`npx eslint .`、`npx vitest run`。evidence 包「post-edit-validation」lead 仍指出 43 编辑事件后无后续验证命令——这是会话级行为证据，与代码级测试覆盖不冲突。 | 引擎核心 pass/fail 边界与高 churn 路径现可被测试捕获；lint error 归零。这是本轮唯一从「未观察」进入「有证据」的维度。 | 会话级 post-edit 验证行为仍为 lead（43 编辑后无后续验证命令），需 Harness session-evidence review；declared ±50-line/complexity 边界仍未机械强制。 |
| Reliable Delivery | 0 observed + 1 connected signal（4 CI workflow 文件）。evidence 包 `episodeCoverage.closedEpisodeCount=0`、`deliveryEvidenceLevels=[]`、`outcomeReview.reviewedCandidateCount=0`。 | evidence 包 DELIVERY EVIDENCE「No bounded evidence observed」+ usageEfficiency.outcomeReview。 | CI 门控 main、rollback 场景存在，但验收仍为 assistant-handoff，无结构化完成/用户纠正信号，57 个长会话候选 0 已复核。 | 可比模型结果证据缺失（`comparableModelOutcomeEvidence=false`）；branch protection / CODEOWNERS 未验证。 |
| Learning Capture | `learningCapture.state="N/A"`，`interventions=[]`，「需要两个可比观察窗口和一次改进对比」。9 个 unscoped Skill 观察但 0 recurring。 | evidence 包 learningCapture + Learning observation coverage（Effectiveness=pending-no-later-window）。 | 本轮修复未被验证为可复用改进——缺少可比后续窗口。 | 无后续可比结果前不可评分；Memory Governance=checked-clean。 |

---

## Asset Health / Repair Progress（6 项 finding 跟踪）

| # | Finding（上次） | 状态 | 证据 |
|---|---|---|---|
| 1 | runStep 失败检测正则无测试 | **Verified** | `claude-scene/src/lib/failure-detection.js` 导出 `detectHandlerFailure`（非字符串返回 false）；`claude-scene/src/commands/failure-detection.test.js` 存在；vitest 226/226 通过。 |
| 2 | 5 个高 churn handler 无聚焦测试 | **Verified** | `design.test.js` / `external-tool-checks.test.js` / `handler-verify.test.js` / `review.test.js` / `ui-tools.test.js` 均存在于 `claude-scene/src/handlers/`。 |
| 3 | 执行日志无 per-run correlation id | **Verified** | `start.js` line 4 `import { randomBytes }`、line 31 `LOG_RUN_ID`、line 36 `ts-randomBytes(3).hex`、line 39 文件名含 run_id 后缀、line 41/47 每行 entry 带 `run_id`。 |
| 4 | declared change-safety 边界未机械强制 | **Partial** | lint error 15→0（`npx eslint .` EXIT=0，21 warnings）。但 `--max-warnings 500` 仍不阻断、cognitive-complexity 仍 per-file 禁用、±50-line 仍靠人工。error 归零是实质改善，机械强制边界仍缺。 |
| 5 | session validation 不闭环到 reviewed check | **Pending** | evidence 包 `withReviewedRelevantCheck` 仍为 0、`closedEpisodeCount=0`、`deliveryEvidenceLevels=[]`。流程问题未动。 |
| 6 | hallmark SKILL.md 3 个断链引用 | **Verified**（超出预期） | Grep `site/css/tokens\|docs/recipes\|docs/study-examples` 在当前 SKILL.md 无匹配；3 个目标文件确认不存在。断链引用已被移除（SKILL.md 当前本地引用全部指向 `references/` 下实际存在的文件）。注：用户背景称此项未修，但独立核对显示已修。 |

**汇总：4 verified / 1 partial / 1 pending**（上次 0/0/6）。

---

## 剩余 Findings（仍存在）

### 1. declared change-safety 边界仍未机械强制（Partial → 仍开放）
- Priority: Medium · Evidence: `npx eslint .` EXIT=0（0 errors, 21 warnings）
- Reason: core-rules.md 声明 ±50-line / 0-new-dependency / ≤130% complexity / test-fail-auto-rollback。lint error 已归零（实质改善），但 `.husky/pre-commit` 仍 `--max-warnings 500` 不阻断、cognitive-complexity 仍 per-file 禁用（start.js 等）、±50-line 仍靠人工 PR review。codeguardian MCP 路径未在静态配置中可见。
- Expected Output: eslint/pre-commit 配置机械强制 declared complexity 与 warning-ceiling 边界，使声明规则产生阻断信号。

### 2. session validation 不闭环到 reviewed check（Pending → 仍开放）
- Priority: Medium · Evidence: evidence 包 `withReviewedRelevantCheck=0`、`closedEpisodeCount=0`、`deliveryEvidenceLevels=[]`、`outcomeReview.reviewedCandidateCount=0`（57 长会话 0 复核）
- Reason: 跨 40 个已分析会话，验收统一为 assistant-handoff（`withStructuredCompletion=0`、`withUserCorrection=0`）；带变更的 episode 未运行绑定到最终变更的 reviewed check。
- Expected Output: 一条带变更的验收路由，把最终变更绑定到 reviewed relevant check 结果，而非仅靠 handoff 声明交付。

### 3. Learning Capture 缺可比后续窗口（未评分）
- Priority: Low · Evidence: `learningCapture.state="N/A"`、`Effectiveness=pending-no-later-window`
- Reason: 本轮 3 项修复（失败检测可测、handler 聚焦测试、run_id）尚未被「可比后续任务结果」验证为可复用改进。需第二个观察窗口。
- Expected Output: 在后续同类任务上观察修复是否复现质量提升，形成 intervention 对比。

### 4. 会话级 post-edit 验证行为 lead（新，来自本轮 evidence）
- Priority: Low · Evidence: evidence 包「Observed 43 edit event(s), but no later validation command in the analyzed sample」
- Reason: 40 个已分析会话中 43 个编辑事件后无后续验证命令（代码级测试覆盖已改善，但会话级行为未变）。需 Harness session-evidence review 检查受影响 episode 的后续验证。
- Expected Output: review 受影响 episode，确认编辑后是否真的跑了验证（pytest lead 54 次是配置级证据，需绑定到具体编辑）。

---

## Evidence and Boundaries

- Episode coverage: 151 episodes, 43 edited, 0 closed, 0 recovered（evidence 包）
- Model: agent-work-loop-v4
- Session selection: stratified; 40 of 83 eligible sessions analyzed; confidence Medium
- Delivery grades observed: 无有界证据
- Source gaps: missing-optional-root（3 enabled roots 仅 1 存在）、partial-secret-scan-coverage（217/220）
- Learning comparison: N/A；0 declared intervention(s)；需可比后续窗口
- 修复证据独立核对：Read `failure-detection.js`、Grep `start.js` LOG_RUN_ID、ls 24 个测试文件、`npx eslint .` EXIT=0、`npx vitest run` 26 文件 226 测试通过、Grep hallmark SKILL.md 断链无匹配

## 整体结论（相比上次）

- **Asset Health 从 0 → 58**：6 项 pending 中 4 项 verified（finding 1/2/3/6）、1 项 partial（finding 4 lint error 归零但机械强制未做）、1 项 pending（finding 5 流程未动）。这是最显著的改善。
- **Loop Effectiveness 从 54 → 62**：Change Validation 维度从「未观察」进入「有证据」，引擎核心 pass/fail 边界与高 churn 路径可测。但 Loop Effectiveness 严格定义为「需可比后续任务结果验证 loop 改进」，本轮尚无该窗口，故仅小幅上升 +8，不因代码修复而跃升。
- **5 维度**：Change Validation 从 Not observed → 有证据（可评 ~55-60 分证据强度，但严格 Loop Effectiveness 口径下保守给分）；其余 4 维度（Task Understanding / Controlled Execution / Reliable Delivery / Learning Capture）证据状态与上次基本一致，仍标 not observed 或保持低证据。
- **最大剩余杠杆**：Reliable Delivery 的 reviewed-check 闭环（finding 5）——这是把交付从 handoff 声明升级为可验证验收的关键，也是 Loop Effectiveness 下一次能显著上升的前提。
